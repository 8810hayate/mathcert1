# 数学検定1級 一次試験（計算技能）模擬試験［第9回］

数学検定1級一次試験（計算技能）対策のための、第9回模擬試験問題（全7問）と詳細な解答解説です。今回の模擬試験では、回転体の体積（バウムクーヘン分割）、行列の対角化可能性と最小多項式、連立線形微分方程式の初期値問題、単位円板への共形写像、ガウスの発散定理を用いたフラックスの計算、巨大べき乗の剰余（フェルマーの小定理）、標本からの母平均の信頼区間推定など、高得点を狙うための重要計算パターンを揃えています。

---

## 模擬試験問題（制限時間：60分）

### 【問題1】微分積分学（回転体の体積・バウムクーヘン分割）
$xy$ 平面上の曲線 $y = \sin x$ と $x$ 軸で囲まれた領域のうち、 $0 \le x \le \pi$ の部分を $y$ 軸の周りに1回転させてできる回転体の体積 $V$ を求めよ。

### 【問題2】線形代数（対角化可能性と最小多項式）
次の $3 \times 3$ 行列 $A$ について、以下の問いに答えよ。
$$A = \begin{pmatrix} 1 & 1 & 0 \\ 0 & 1 & 1 \\ 0 & 0 & 2 \end{pmatrix}$$
1. 行列 $A$ が対角化可能であるか判定せよ。
2. 行列 $A$ の最小多項式 $m(x)$ を求めよ。

### 【問題3】微分方程式（連立線形微分方程式）
次の連立微分方程式の初期値問題を解け。
$$\begin{cases} 
\frac{dx}{dt} = 3x - 2y \\ 
\frac{dy}{dt} = 2x - 2y 
\end{cases} \quad \left( x(0) = 3, \; y(0) = 2 \right)$$

### 【問題4】複素解析（共形写像・単位円板への写像）
複素平面の上半平面 $\operatorname{Im}(z) > 0$ を、複素 $w$ 平面の単位円板 $|w| < 1$ に共形（等角）に写し、かつ $z = i$ が $w = 0$ に写るような一次分数変換（メビウス変換） $w = f(z)$ の1つを求めよ。

### 【問題5】ベクトル解析（ガウスの発散定理とフラックス）
ベクトル場 $\mathbf{F}(x, y, z) = (x^3, \; y^3, \; z^3)$ に対し、 $S$ を半径 $R$ の球面 $x^2 + y^2 + z^2 = R^2$ とし、 $\mathbf{n}$ を外向き単位法線ベクトルとする。ガウスの発散定理を用いて、次の面積分（フラックス）の値を求めよ。
$$I = \iint_S \mathbf{F} \cdot \mathbf{n} \,dS$$

### 【問題6】代数学・整数論（フェルマーの小定理と剰余）
$2^{1000}$ を $13$ で割ったときの余りを求めよ。

### 【問題7】確率・統計（母平均の区間推定）
標準偏差 $\sigma = 16$ である正規分布に従う母集団から、大きさ $n=64$ の標本をランダムに抽出したところ、標本平均は $\bar{x} = 100$ であった。
この母集団の母平均 $\mu$ に対する信頼水準 $95\%$ の信頼区間を求めよ。
ただし、標準正規分布の上側 $2.5\%$ 点を $z_{0.025} = 1.96$ とする。

---
---

## 解答・解説

### 【問題1】の解答・解説
#### 解答
$$V = 2\pi^2$$

#### 詳細な計算ステップ
1. **バウムクーヘン分割（円筒殻法）の公式の導入**
   $y = f(x) \ge 0$ と $x$ 軸、および $x=a, x=b$ ($0 \le a < b$) で囲まれた領域を $y$ 軸の周りに1回転させてできる回転体の体積 $V$ は、次の公式（バウムクーヘン分割）で計算されます：
   $$V = 2\pi \int_a^b x f(x) \,dx$$
   本問では $f(x) = \sin x$ 、 $a = 0$ 、 $b = \pi$ です：
   $$V = 2\pi \int_0^{\pi} x \sin x \,dx$$

2. **部分積分の実行**
   積分部分 $I = \int_0^{\pi} x \sin x \,dx$ を部分積分法を用いて計算します。 $\sin x = (-\cos x)'$ であることを利用します：
   $$I = \int_0^{\pi} x (-\cos x)' \,dx = \Big[ x (-\cos x) \Big]_0^{\pi} - \int_0^{\pi} 1 \cdot (-\cos x) \,dx$$
   $$I = \Big[ -x \cos x \Big]_0^{\pi} + \int_0^{\pi} \cos x \,dx$$

3. **各項の計算**
   * 第1項：
     $$\Big[ -x \cos x \Big]_0^{\pi} = (-\pi \cos \pi) - (0) = -\pi(-1) = \pi$$
   * 第2項：
     $$\int_0^{\pi} \cos x \,dx = \Big[ \sin x \Big]_0^{\pi} = \sin \pi - \sin 0 = 0 - 0 = 0$$
   したがって：
   $$I = \pi + 0 = \pi$$

4. **最終計算**
   公式に代入して体積 $V$ を求めます：
   $$V = 2\pi I = 2\pi^2$$

---

### 【問題2】の解答・解説
#### 解答
1. 対角化可能ではない。
2. $m(x) = (x-1)^2(x-2)$ （または $m(A) = (A-I)^2(A-2I) = O$）

#### 詳細な計算ステップ
1. **固有値と固有多項式の計算**
   行列 $A = \begin{pmatrix} 1 & 1 & 0 \\ 0 & 1 & 1 \\ 0 & 0 & 2 \end{pmatrix}$ は上三角行列であるため、対角成分がそのまま固有値となります。
   特性多項式 $p(\lambda)$ は次の通りです：
   $$p(\lambda) = \det(A - \lambda I) = (1-\lambda)^2(2-\lambda) = -(\lambda-1)^2(\lambda-2)$$
   したがって、固有値は以下の通りです：
   * $\lambda = 1$ （重複度 2）
   * $\lambda = 2$ （重複度 1）

2. **対角化可能性の判定**
   固有値 $\lambda = 1$ に対する固有空間の次元（幾何学的重複度） $d_1$ を調べます：
   $$A - I = \begin{pmatrix} 0 & 1 & 0 \\ 0 & 0 & 1 \\ 0 & 0 & 1 \end{pmatrix}$$
   この行列の階数（rank）は、明らかに $2$ です（第2行と第3行は実質的に同一であり、第1行と第2行が一次独立なため）。
   次元定理（Rank-Nullity Theorem）より：
   $$d_1 = \dim \operatorname{Ker}(A - I) = 3 - \operatorname{rank}(A - I) = 3 - 2 = 1$$
   固有値 $\lambda = 1$ の代数的重複度（特性多項式における重根の数）は $2$ であるのに対し、幾何学的重複度（一次独立な固有ベクトルの数）は $1$ です。
   代数的重複度と幾何学的重複度が一致しないため、行列 $A$ は**対角化可能ではありません**。

3. **最小多項式 $m(x)$ の決定**
   最小多項式 $m(x)$ は、特性多項式と同じ固有値を根に持ち、かつ $m(A) = O$ を満たす最小次数の単項式です。
   特性多項式の因数から、最小多項式の候補は次の2つに絞られます：
   * 候補1： $g_1(x) = (x-1)(x-2)$
   * 候補2： $g_2(x) = (x-1)^2(x-2)$
   
   実際に $g_1(A)$ を計算してみます：
   $$(A-I)(A-2I) = \begin{pmatrix} 0 & 1 & 0 \\ 0 & 0 & 1 \\ 0 & 0 & 1 \end{pmatrix} \begin{pmatrix} -1 & 1 & 0 \\ 0 & -1 & 1 \\ 0 & 0 & 0 \end{pmatrix} = \begin{pmatrix} 0 & -1 & 1 \\ 0 & 0 & 0 \\ 0 & 0 & 0 \end{pmatrix} \neq O$$
   $g_1(A) \neq O$ であるため、最小多項式は $g_1(x)$ ではありません。
   ハミルトン・ケーリーの定理より特性多項式では必ず $p(A) = O$ となるため、残った $g_2(x)$ が最小多項式となります：
   $$m(x) = (x-1)^2(x-2)$$

---

### 【問題3】の解答・解説
#### 解答
$$x(t) = \frac{8}{3}e^{2t} + \frac{1}{3}e^{-t}, \quad y(t) = \frac{4}{3}e^{2t} + \frac{2}{3}e^{-t}$$

#### 詳細な計算ステップ
1. **行列形式での定式化と固有値の算出**
   連立微分方程式を $\frac{d\mathbf{x}}{dt} = M \mathbf{x}$ の形で表します：
   $$\frac{d}{dt}\begin{pmatrix} x \\ y \end{pmatrix} = \begin{pmatrix} 3 & -2 \\ 2 & -2 \end{pmatrix} \begin{pmatrix} x \\ y \end{pmatrix}$$
   係数行列 $M = \begin{pmatrix} 3 & -2 \\ 2 & -2 \end{pmatrix}$ の固有値 $\lambda$ を求めます：
   $$\det(M - \lambda I) = \begin{vmatrix} 3-\lambda & -2 \\ 2 & -2-\lambda \end{vmatrix} = (3-\lambda)(-2-\lambda) - (-4) = \lambda^2 - \lambda - 2 = 0$$
   因数分解して固有値を求めます：
   $$(\lambda - 2)(\lambda + 1) = 0 \implies \lambda = 2, \; -1$$

2. **固有ベクトルの算出**
   * **$\lambda_1 = 2$ に対する固有ベクトル $\mathbf{v}_1$**：
     $$(M - 2I)\mathbf{v}_1 = \mathbf{0} \implies \begin{pmatrix} 1 & -2 \\ 2 & -4 \end{pmatrix} \begin{pmatrix} v_{11} \\ v_{12} \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \end{pmatrix} \implies v_{11} - 2v_{12} = 0$$
     固有ベクトルとして以下を選びます：
     $$\mathbf{v}_1 = \begin{pmatrix} 2 \\ 1 \end{pmatrix}$$
   * **$\lambda_2 = -1$ に対する固有ベクトル $\mathbf{v}_2$**：
     $$(M + I)\mathbf{v}_2 = \mathbf{0} \implies \begin{pmatrix} 4 & -2 \\ 2 & -1 \end{pmatrix} \begin{pmatrix} v_{21} \\ v_{22} \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \end{pmatrix} \implies 2v_{21} - v_{22} = 0$$
     固有ベクトルとして以下を選びます：
     $$\mathbf{v}_2 = \begin{pmatrix} 1 \\ 2 \end{pmatrix}$$

3. **一般解の構成**
   一般解は固有値と固有ベクトルを用いて次のように表されます：
   $$\begin{pmatrix} x(t) \\ y(t) \end{pmatrix} = C_1 e^{2t} \begin{pmatrix} 2 \\ 1 \end{pmatrix} + C_2 e^{-t} \begin{pmatrix} 1 \\ 2 \end{pmatrix}$$
   成分ごとに書くと：
   $$x(t) = 2C_1 e^{2t} + C_2 e^{-t} \quad \cdots \text{(1)}$$
   $$y(t) = C_1 e^{2t} + 2C_2 e^{-t} \quad \cdots \text{(2)}$$

4. **初期条件による定数の決定**
   $t = 0$ における初期値 $x(0) = 3$, $y(0) = 2$ を代入します：
   * 式 (1) より： $2C_1 + C_2 = 3 \quad \cdots \text{(3)}$
   * 式 (2) より： $C_1 + 2C_2 = 2 \quad \cdots \text{(4)}$
   
   この連立一次方程式を解きます。
   式 (4) より $C_1 = 2 - 2C_2$ とし、これを式 (3) に代入します：
   $$2(2 - 2C_2) + C_2 = 3 \implies 4 - 3C_2 = 3 \implies 3C_2 = 1 \implies C_2 = \frac{1}{3}$$
   これより：
   $$C_1 = 2 - 2\left(\frac{1}{3}\right) = \frac{4}{3}$$

5. **特解の決定**
   定数の値を代入して一般解を決定します：
   $$x(t) = 2\left(\frac{4}{3}\right)e^{2t} + \frac{1}{3}e^{-t} = \frac{8}{3}e^{2t} + \frac{1}{3}e^{-t}$$
   $$y(t) = \frac{4}{3}e^{2t} + 2\left(\frac{1}{3}\right)e^{-t} = \frac{4}{3}e^{2t} + \frac{2}{3}e^{-t}$$

---

### 【問題4】の解答・解説
#### 解答
$$w = \frac{z - i}{z + i}$$

#### 詳細な計算ステップ
1. **上半平面を単位円板に写す一次分数変換の一般形**
   複素平面の上半平面 $\operatorname{Im}(z) > 0$ を単位円板 $|w| < 1$ に写す一次分数変換は、複素数 $\alpha$ （ただし $\operatorname{Im}(\alpha) > 0$）と実数 $\theta$ を用いて次の一般式で表されます：
   $$w = e^{i\theta} \frac{z - \alpha}{z - \bar{\alpha}}$$
   ここで、分子の零点 $z = \alpha$ は $w = 0$ に写る点を表し、分母の極 $z = \bar{\alpha}$ は $w = \infty$ に写る対称点を表します。

2. **条件の適用**
   問題文の条件より、 $z = i$ が $w = 0$ に写る必要があるため、零点を決定できます：
   $$\alpha = i$$
   $\operatorname{Im}(i) = 1 > 0$ なので、これは上半平面の点であり適当です。その複素共役は $\bar{\alpha} = -i$ となります。
   これらを一般式に代入します：
   $$w = e^{i\theta} \frac{z - i}{z + i}$$

3. **回転因子の決定と1つの変換の抽出**
   問題では「1つを求めよ」とされているため、計算を最もシンプルにするために $e^{i\theta} = 1$ （すなわち $\theta = 0$）と選ぶことができます。
   これにより、次の変換式が得られます：
   $$w = \frac{z - i}{z + i}$$

4. **境界が単位円に写るかどうかの確認**
   実軸上の点 $z = x \in \mathbb{R}$ の写り先の絶対値 $|w|$ を確認します：
   $$|w| = \left| \frac{x - i}{x + i} \right| = \frac{|x - i|}{|x + i|} = \frac{\sqrt{x^2 + (-1)^2}}{\sqrt{x^2 + 1^2}} = \frac{\sqrt{x^2+1}}{\sqrt{x^2+1}} = 1$$
   実軸（上半平面の境界）がすべて単位円 $|w| = 1$ に写っているため、この変換は条件を満たしています。

---

### 【問題5】の解答・解説
#### 解答
$$\frac{12}{5}\pi R^5$$

#### 詳細な計算ステップ
1. **ガウスの発散定理の適用**
   ベクトル場 $\mathbf{F}$ の閉曲面 $S$ （球）を通る外向きのフラックスは、球の内部の領域 $V$ における発散（divergence）の体積積分に等しくなります：
   $$\iint_S \mathbf{F} \cdot \mathbf{n} \,dS = \iiint_V \nabla \cdot \mathbf{F} \,dV$$

2. **発散（divergence） $\nabla \cdot \mathbf{F}$ の計算**
   $\mathbf{F}(x, y, z) = (x^3, y^3, z^3)$ より：
   $$\nabla \cdot \mathbf{F} = \frac{\partial(x^3)}{\partial x} + \frac{\partial(y^3)}{\partial y} + \frac{\partial(z^3)}{\partial z} = 3x^2 + 3y^2 + 3z^2 = 3(x^2 + y^2 + z^2)$$

3. **3重積分の計算（球面座標系への変換）**
   積分領域 $V$ は半径 $R$ の球の内部 $x^2 + y^2 + z^2 \le R^2$ です。
   球面座標系 $x = \rho \sin\phi\cos\theta, \; y = \rho \sin\phi\sin\theta, \; z = \rho \cos\phi$ を用いて積分を書き換えます。
   このとき、被積分関数は $3(x^2 + y^2 + z^2) = 3\rho^2$ であり、ヤコビアンは $\rho^2 \sin\phi$ です：
   $$I = \iiint_V 3(x^2 + y^2 + z^2) \,dx\,dy\,dz = \int_0^{2\pi} \int_0^{\pi} \int_0^R (3\rho^2) \cdot \rho^2 \sin\phi \,d\rho\,d\phi\,d\theta$$
   $$I = \int_0^{2\pi} \int_0^{\pi} \int_0^R 3\rho^4 \sin\phi \,d\rho\,d\phi\,d\theta$$

4. **各変数の積分の計算**
   変数は独立して積分可能です：
   $$I = 3 \left( \int_0^{2\pi} d\theta \right) \left( \int_0^{\pi} \sin\phi \,d\phi \right) \left( \int_0^R \rho^4 \,d\rho \right)$$
   * **$\theta$ の積分**：
     $$\int_0^{2\pi} d\theta = 2\pi$$
   * **$\phi$ の積分**：
     $$\int_0^{\pi} \sin\phi \,d\phi = \Big[ -\cos\phi \Big]_0^{\pi} = -(-1) - (-1) = 2$$
   * **$\rho$ の積分**：
     $$\int_0^R \rho^4 \,d\rho = \left[ \frac{\rho^5}{5} \right]_0^R = \frac{R^5}{5}$$

5. **積の計算**
   $$I = 3 \times 2\pi \times 2 \times \frac{R^5}{5} = \frac{12}{5}\pi R^5$$

---

### 【問題6】の解答・解説
#### 解答
$$3$$

#### 詳細な計算ステップ
1. **合同式の立式とフェルマーの小定理**
   $2^{1000} \pmod{13}$ を求めます。
   法 $p = 13$ は素数であり、 $2$ と $13$ は互いに素であるため、フェルマーの小定理 $a^{p-1} \equiv 1 \pmod p$ を適用できます：
   $$2^{12} \equiv 1 \pmod{13}$$

2. **べき指数の簡単化**
   指数 $1000$ を $12$ で割り、商と余りに分解します：
   $$1000 = 12 \times 83 + 4$$
   これを利用してべき乗を整理します：
   $$2^{1000} = (2^{12})^{83} \times 2^4$$

3. **余りの計算**
   両辺で $\pmod{13}$ をとります。 $2^{12} \equiv 1$ より：
   $$2^{1000} \equiv (1)^{83} \times 2^4 \equiv 1 \times 16 \pmod{13}$$
   $16$ を $13$ で割った余りを求めます：
   $$16 \equiv 3 \pmod{13}$$
   したがって、余りは **3** です。

---

### 【問題7】の解答・解説
#### 解答
$$[96.08, \; 103.92] \quad (\text{または } 96.08 \le \mu \le 103.92)$$

#### 詳細な計算ステップ
1. **母平均 $\mu$ の区間推定の公式**
   母標準偏差 $\sigma$ が既知の正規分布に従う母集団から得られた標本（大きさ $n$ 、平均 $\bar{x}$ ）に対し、信頼水準 $1-\alpha$ の信頼区間は次のようになります：
   $$\left[ \bar{x} - z_{\alpha/2} \frac{\sigma}{\sqrt{n}}, \quad \bar{x} + z_{\alpha/2} \frac{\sigma}{\sqrt{n}} \right]$$

2. **パラメータ値の代入**
   信頼水準 $95\%$ より、両側 $5\%$ に対応する棄却点 $z_{0.025} = 1.96$ を用います。
   各統計量を代入します：
   * 標本平均： $\bar{x} = 100$
   * 母標準偏差： $\sigma = 16$
   * 標本サイズ： $n = 64 \implies \sqrt{n} = \sqrt{64} = 8$

3. **許容誤差（誤差の幅）の計算**
   $$z_{0.025} \frac{\sigma}{\sqrt{n}} = 1.96 \times \frac{16}{8} = 1.96 \times 2 = 3.92$$

4. **信頼区間の算出**
   * 下限： $100 - 3.92 = 96.08$
   * 上限： $100 + 3.92 = 103.92$
   したがって、母平均 $\mu$ に対する $95\%$ 信頼区間は次のようになります：
   $$[96.08, \; 103.92]$$
